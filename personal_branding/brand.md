# Personal Brand — Daniel Duhnev

## Positioning

> **I build production ML and GenAI systems - and the infrastructure that keeps them running.**

ML / MLOps engineer. The blend that differentiates: hands-on model/GenAI
delivery plus real platform-engineering depth (4 years as an SRE at Sky
before moving into ML/AI roles).

Target roles: Machine Learning Engineer · MLOps Engineer · Data Scientist.
Audience: hiring managers at Barcelona-based or remote-friendly companies.

## Visual identity

- **Palette**: forest/teal accent (`#059669` / `#065f46` / `#ecfdf5`) on the
  same near-white/slate-ink base as the site this was bootstrapped from —
  deliberately different hue so the two sites don't read as templated from
  each other. All tokens live directly in `src/styles/global.css` `:root`;
  no separate `palette.json` — not worth the extra indirection at this scale.
- **Typography**: Inter (variable) for reading text, JetBrains Mono for
  dates/stack tags/eyebrows — inherited as-is from the source template.
- **Motif**: the pipeline-rail timeline is kept — it's a generic
  deployment/DAG visual, not exclusive to any one person's brand.
